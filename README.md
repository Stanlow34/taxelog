# taxelog
Taxe log

https://git-scm.com/docs

ajouter : git add "filename"

modifier la branch : git commit -m "First"

vérifier : git status

pousser : git push --set-upstream taxelog master

récup : git pull

git commit -m "First"

Server : node server.js